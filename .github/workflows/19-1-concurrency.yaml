name: 19-1- Managing concurrency
on:
    workflow_dispatch:

concurrency: 
    group: ${{ github.workflow }}-${{ github.ref }}
jobs:
  ping-url:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Ping URL
        uses: ./.github/actions/docker-ping-url
        with:
          url: https://www.invalid-abacedfdfdfdfdf.com
          max_trails: 20
          delay: 5
         